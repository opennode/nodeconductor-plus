Plan customer connection list
-----------------------------

To get a list of plan-customer connections run GET against **/api/plan-customers/** as authenticated user.
Plan customers can be filtered by customer UUID:

?customer=<customer_UUID>
